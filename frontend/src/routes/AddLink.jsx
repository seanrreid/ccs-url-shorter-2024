import Wrapper from '../components/Wrapper';

const AddLink = () => {
    return (
        <Wrapper>
            <h1>Add a Link</h1>
            <p>Add Link Component should be protected</p>
        </Wrapper>
    );
};

export default AddLink;
